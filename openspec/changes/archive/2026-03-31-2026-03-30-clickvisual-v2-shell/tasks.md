# 任务：ClickVisual v2 独立前端壳层

## 真源规则

本文件是当前 v2 工作的任务真源。`task_plan.md`、`progress.md`、`findings.md` 只记录编排状态、执行细节和恢复信息，不维护竞争性的主任务清单。

## 当前执行状态

- 当前 active task：T9
- 当前状态：done
- 当前 owner：A 完成收口与验收
- 下一步：当前分配范围已完成；如继续推进，将进入下一轮能力扩展或真实接口接入

## 任务列表

- [x] T1 建立 `ui-v2/` 应用壳层，完成路由、providers 配置和基础工程配置
- [x] T2 建立版本共存与切换基础能力，覆盖 `v1/v2` 入口、切换器、偏好记忆与回退路径
- [x] T3 建立报表优先的平台共享层，先覆盖报表主链路所需的布局、导航、时间范围和通用页面状态
- [x] T4 优先定义定时任务报表数据推送所需的 `v2` 契约层与 mock 数据，覆盖报表列表、报表配置、调度、预览和钉钉推送
- [x] T5 优先实现 `report` 垂直业务域，打通定时任务报表数据推送主链路
- [x] T6 补齐报表主链路相关的错误处理、任务状态反馈、降级行为与版本切换链路验证
- [x] T7 在报表主链路稳定后，按 `v2/stitch/` 统一视觉重做五个模块页面，先完成共享视觉底座与真实路由页替换
- [x] T8 为剩余模块补齐 AI 动作入口、统一错误处理和降级行为
- [x] T9 验证阶段性里程碑：先完成报表数据推送主链路验收，再完成完整壳子验收

## 依赖说明

- T1 是所有实现任务的前置条件
- T2 依赖 T1
- T3 依赖 T1 和 T2
- T4 依赖 T1，并优先为 T5-T6 提供报表契约基础
- T5 依赖 T3 和 T4
- T6 依赖 T5
- T7 依赖 T3、T4 和 T6
- T8 依赖 T7
- T9 依赖 T2-T8

## 分派说明

- 在 T1-T4 完成前，优先串行执行
- T5 和 T6 是当前最高优先级主链路，优先于其它业务域
- 只有在报表数据推送主链路稳定后，才释放 T7 和 T8
- T7 必须以统一视觉底座优先，不允许五个模块各自独立长歪
- 只有在存在验证证据后，才能勾选任务完成
